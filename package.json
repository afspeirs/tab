{
	"name": "@afspeirs/tab",
	"version": "0.5.0",
	"description": "A simple tab module",
	"license": "MIT",
	"repository": "afspeirs/tab",
	"main": "lib/index.js",
	"keywords": [
		"npm",
		"package",
		"afspeirs",
		"tab"
	],
	"scripts": {
		"build": "babel src --out-dir lib",
		"version": "npm run build && git add -A src",
		"postversion": "git push && git push --tags"
	},
	"babel": {
		"presets": [
			"es2015"
		]
	},
	"devDependencies": {
		"babel-cli": "^6.26.0",
		"babel-core": "^6.26.3",
		"babel-preset-es2015": "^6.24.1"
	}
}
